<plan>
<step>Install `react-dropzone` library via npm.</step>
<step>Create a new client component `components/PhotoUploader.tsx`.</step>
<step>Implement a drag-and-drop zone with visual feedback (hover states, dashed borders) matching the dark Glassmorphism aesthetic.</step>
<step>Configure the dropzone to accept only image formats and handle up to 100 files at once.</step>
<step>Export the component so it can emit the selected files to its parent.</step>
</plan>

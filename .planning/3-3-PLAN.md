<plan>
<step>In `app/page.tsx`, implement the `complete` UI state logic triggered when the AI finishes its simulation.</step>
<step>Display the photos separated into two distinct masonry/grid sections: "AI Curated Selection" (Prominent) and "Hidden/Removed" (Smaller, muted thumbnails).</step>
<step>Add hover actions on the "Hidden/Removed" section allowing the user to click a "Restore" button, which moves the photo back to the Curated array.</step>
</plan>

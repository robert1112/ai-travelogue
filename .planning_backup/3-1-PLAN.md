<plan>
<step>Create a new utility file `lib/ai-curator.ts`.</step>
<step>Implement an asynchronous function `simulateAICuration(photos)`.</step>
<step>Inside the function, add a simulated `Promise` delay based on the number of photos to mimic AI processing time.</step>
<step>Randomly divide the photo array into two categories: `curated` (70%) and `rejected` (30%), adding a mock "reason" (e.g. "blurry", "duplicate") for rejected photos.</step>
<step>Return the categorized sets.</step>
</plan>
